// Stub usado só pelos testes (vitest.config.ts) — o Next.js resolve o
// pacote real "server-only" via alias interno do próprio webpack, que o
// Vite/Vitest não conhece. Este stub é um no-op equivalente.
export {};
